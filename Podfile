# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'AEXCode' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for AEXCode

  pod 'Promissum', :git => 'https://github.com/tomlokhorst/Promissum.git' , :branch => 'swift3'
  
  target 'AEXCodeTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'AEXCodeUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
